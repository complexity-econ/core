name: Core Engine Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install sbt
        run: |
          curl -fsSL "https://github.com/sbt/sbt/releases/download/v1.10.7/sbt-1.10.7.tgz" | sudo tar xz -C /usr/local
          echo "/usr/local/sbt/bin" >> "$GITHUB_PATH"

      - name: Check formatting
        run: sbt scalafmtCheckAll

      - name: Run tests
        run: sbt test

      - name: Build fat JAR
        run: sbt assembly

      - name: Smoke test (3 seeds)
        run: java -jar target/scala-3.5.2/sfc-abm.jar 2000 3 ci_smoke pln
